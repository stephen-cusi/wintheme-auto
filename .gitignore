# Rust 构建产物
/target

# 本地运行日志（CI 不需要）
*.log

# 编辑器/系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 临时 / 一次性脚本（避免误提交）
scripts/inspect_api.py

# 本仓库开发/维护注意事项（本地维护，不上传）
AGENTS.md

# 第三方库（path 依赖，本地开发用，不提交）
win32_notif/
win32_notif-master.zip
